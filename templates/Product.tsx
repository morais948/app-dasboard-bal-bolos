import { View, Text } from 'react-native'

function Product() {
    return (
        <View style={{
            flex: 1,
            justifyContent: "center",
            alignItems: "center",
        }}>
            <Text>Produtos</Text>
        </View>
    );
}

export default Product